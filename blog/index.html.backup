---
layout: page
custom_css: blog/blog.css

title: blog
description:
image: /assets/dylowicz.jpg

header: blog
---

<div class="posts">
  {% for post in site.posts %}
    <article class="post-preview" onclick="location.href='{{ site.baseurl }}{{ post.url }}';">

      <h1><a href="{{ site.baseurl }}{{ post.url }}">{{ post.title }}</a></h1>

      <div class="date">{{ post.date | date: "%b %e, %Y" }} &middot; {{ post.read_time }} min read</div>

      <div class="excerpt">
        <p>{{ post.excerpt }}</p>
      </div>

      <a href="{{ site.baseurl }}{{ post.url }}" class="link-small">Read more →</a>
    </article>
  {% endfor %}
</div>
